import { Typography, Box} from "@mui/material";

const Products = () => {
    return (
        <Box>
            <Typography variant="h4" component="h1" color="secondary" sx={{ mb: 3 }}>
                 Товари
            </Typography>
        </Box>
    );
}

export default Products;